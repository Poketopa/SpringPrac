spring:
  datasource:
    url: jdbc:mysql://localhost:3306/miniblog?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    encoding:
      force-response: true

server:
  port: 8080